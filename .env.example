# ============================================================================
# TURSO DATABASE CONFIGURATION
# ============================================================================
# Required for both Python and TypeScript database clients

# Your Turso database URL
# Format: libsql://[database-name]-[org-name].turso.io
# Get this from: turso db show <database-name>
TURSO_DATABASE_URL=libsql://your-database-name.turso.io

# Your Turso authentication token
# Get this from: turso db tokens create <database-name>
TURSO_AUTH_TOKEN=eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9...your-token-here

# Optional: For embedded replicas (local-first development)
# TURSO_SYNC_URL=file:./local.db
# TURSO_SYNC_INTERVAL=60

# Optional: Database connection configuration
# TURSO_CONNECTION_TIMEOUT=30
# TURSO_REQUEST_TIMEOUT=10

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

NODE_ENV=development
LOG_LEVEL=info

# ============================================================================
# PYTHON CONFIGURATION
# ============================================================================

PYTHONPATH=src/python

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Create a Turso database (if you haven't already):
#    turso db create resume-tracker
#
# 2. Get your database URL:
#    turso db show resume-tracker
#
# 3. Create an authentication token:
#    turso db tokens create resume-tracker
#
# 4. Copy this file to .env and fill in your values:
#    cp .env.example .env
#
# 5. Initialize the database schema:
#    turso db shell resume-tracker < src/python/db/migrations/001_initial_schema.sql
#
# 6. Verify the setup:
#    turso db shell resume-tracker "SELECT COUNT(*) FROM sqlite_master WHERE type='table'"
#
# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# - NEVER commit your .env file to version control
# - Add .env to your .gitignore file
# - Rotate tokens regularly for security
# - Use different tokens for development and production
# - Consider using environment-specific .env files:
#   - .env.development
#   - .env.production
#   - .env.test
#
# ============================================================================
